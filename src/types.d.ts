/**
 * @typedef {import('react').ReactNode} ReactNode
 * @typedef {Object} WithChildren
 * @property {ReactNode} children
 */

// Augment React types to include children in forwardRef component props
